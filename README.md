CSS-transition-bubblegrow
=========================

Use CSS to display a &lt;div> as a circle and then use CSS transition to grow the circle from 0 to an oversized circle and then decrease to optimum sized circle.
